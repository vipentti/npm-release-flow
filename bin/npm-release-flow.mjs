#!/usr/bin/env node
// Placeholder CLI entry point. The CLI planlet implements the prepare/tag/check
// command set (blueprint §8). Until then every invocation is an error.
console.error("npm-release-flow: not implemented yet (initial setup only)");
console.error("usage: npm-release-flow <prepare|tag|check> [--execute]");
process.exit(1);
