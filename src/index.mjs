// Placeholder programmatic entry point. The CLI planlet replaces this with the
// package's exported surface (blueprint §3: CLI plus the workflow YAML as data,
// zero runtime dependencies, plain .mjs with no build step).
export {};
