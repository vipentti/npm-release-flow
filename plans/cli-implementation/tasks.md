# Tasks: CLI (prepare/tag/check) and workflow job bodies for npm-release-flow

Blueprint refs (§N) throughout: `vipentti/agent-blueprints` @ `f9d06c77b9920e4cfab774d254f22d60894c1f05`, file `blueprints/npm-release-flow.md`. Behavior reference: `vipentti/planlet` (`scripts/release.mjs`, `scripts/detect-release-merge.mjs`, `.github/workflows/release.yml`).

- [x] T1 Spawn helper and error contract: `src/lib/spawn.mjs`, the one subprocess module (sync + async variants, captures stdout/stderr, non-zero exit -> typed error carrying stdout/stderr plus the numeric exit status and the terminating signal when applicable; Windows `.cmd` resolution once: win32 spawns through the shell per PATHEXT, POSIX spawns argv directly; no other module spawns), and `src/lib/errors.mjs` (exit-code taxonomy 0 success / 1 error / 2 no-op, and the error-content contract: every failure message states what was checked, what state was found, and the correction action). Ref-probe semantics (used by the `git ls-remote --exit-code` probes in T3/T4/T9): only exit status 2 means "ref absent"; every other nonzero status (auth/network failures) propagates as an error, never as absent. Verify: `node --test` unit tests (success, failure, non-zero exit, stderr capture; a test proving status 2 and another nonzero status survive through the helper and are distinguishable; stub-command test for the win32 path).
- [x] T2 Core release-state modules: `src/lib/versions.mjs` (stable X.Y.Z parse/compare, strict increase), `src/lib/control-files.mjs` (release-diff allowlist = exactly CHANGELOG.md/package.json/package-lock.json; package.json vs lockfile identity; lockfile version fields root `version` + `packages[""]`), `src/lib/changelog.mjs` (Keep a Changelog grammar: non-empty `## [Unreleased]` detection, cut into `## [x.y.z] - date`, link-reference and compare-URL rewrite, version-section extraction for release notes), `src/lib/release-state.mjs` (the §9 enumeration classification over a git diff: version unchanged -> ordinary; stable increase + exact 3-file diff + valid changelog + no tag or tag at release commit -> valid; every ambiguous row -> hard fail with the checked/found/correction message). Verify: `node --test` matrix tests covering every §9 row.
- [x] T3 `prepare` command: `src/commands/prepare.mjs` implementing the concept-doc flow: clean worktree + HEAD at `origin/main`; `--version X.Y.Z` required and stable (syntax only); resolve the matching release PR state BEFORE strict-increase validation: open or merged PR -> exit 2 even when `main` already has the requested version; closed-unmerged PR -> exit 1; only with no matching PR: version strictly greater than current, no conflicting local/remote `release/vX.Y.Z` branch, no conflicting tag; non-empty `[Unreleased]` section; read-only signing preflight proving Git's configured commit-signing key is locally available and usable before the first mutation (dry-run reports it; `--execute` refuses without it); dry-run (default) prints every would-mutation and stops; `--execute` cuts the changelog, bumps the three version fields, revalidates, creates the branch, creates the signed commit `release: X.Y.Z` (`git commit -S`, refuses unsigned), verifies post-commit (message, one parent, `git verify-commit`, changed-file set exactly the three control files), pushes, `ls-remote`-verifies the remote ref, creates the PR via `gh pr create` with the machine-readable kit-version skew marker line in the body (§10), returns to `main`. Rerun refusals per §9 boundaries 1-2 with distinct messages (local branch / remote branch / open PR / merged "version already prepared" / closed-unmerged manual) and explicit precedence: an open or merged PR for `release/vX.Y.Z` wins and exits 2 regardless of matching local/remote branch presence (a successful `prepare` leaves the local and remote release branches while the open PR exists); a closed-unmerged PR exits 1; only when no matching PR exists do local or remote release branches count as exit-1 conflicts. Partial failure reports what was created and what remains manual. Verify: fixture-repo integration tests (local bare remote, PATH-shimmed `gh`, real git) asserting dry-run non-mutation, full execute, refusals, exit-code mapping (2 for open-PR/version-already-prepared, 1 for conflicting local/remote branch and closed-unmerged PR), error-content strings, and combined-state fixtures (open PR plus local/remote branches -> 2; merged PR plus retained branches -> 2; merged PR where `main` already has the requested version -> 2; closed-unmerged PR with branches -> 1; branches only -> 1; missing signing key and wrong fingerprint/key -> refusal before any file mutation); `node --test`.
- [x] T4 `tag` command: `src/commands/tag.mjs`, break-glass local recovery (concept doc; separate from the automated path): `--version X.Y.Z` required; before any mutation the command proves read-only that a usable secret key for `NPM_RELEASE_FLOW_GPG_FINGERPRINT` exists in the local GPG home (signing preflight; wrong-fingerprint or missing-key -> refusal); the tag target is explicitly the release-merge commit on `origin/main` (the merged release PR's commit, the same commit the automated path tags; `prepare`'s signed branch commit is not the target), resolved before any tag creation deterministically: clean worktree and `origin/main` fetched; walk `origin/main`'s first-parent history and find exactly one matching release merge for the requested version using the documented merge-message grammar (`Merge pull request #N from <owner>/release/v<version>`), zero or multiple candidates -> refusal; run the §9 release-state classification on the candidate commit against its first parent and require a valid release for the requested version (a release-like merge message whose diff is not a valid release -> refusal); validate `package.json`, `package-lock.json`, and changelog from the candidate commit, not the current worktree (version fields match the requested version, lockfile identity, valid released section `## [x.y.z] - date`); wrong target -> refusal. Dry-run prints the plan; `--execute` creates the annotated signed tag `vX.Y.Z` (subject `Release vX.Y.Z`) on the resolved target as create-then-verify-then-push (§6, no partial-mutation window), fully verifies it (annotated object, target = release-merge commit, subject, `git verify-tag --raw` exactly one `VALIDSIG` whose primary fingerprint equals env `NPM_RELEASE_FLOW_GPG_FINGERPRINT` (40-hex required)), pushes authenticated as the release GitHub App (T11 holds on the recovery path too; an operator-credential push cannot create the tag under the `refs/tags/v*` creation ruleset, whose only bypass actor is the release App): `NPM_RELEASE_FLOW_APP_ID` read from the repository variable via `gh api`, `NPM_RELEASE_FLOW_APP_PRIVATE_KEY` from the local environment (secrets are never API-readable; non-empty asserted), installation token minted by the shared `src/lib/app-token.mjs` helper (same JWT + API exchange as the pinned `actions/create-github-app-token`), push authenticated with the token; refuses when the material is absent or the token cannot be minted; verifies the remote object equals the local one. Refuses unsigned or lightweight tags. Completion semantics per §9 boundary 4: remote valid tag -> fetch + full verify, exit 0; remote divergent tag -> exit 1; remote absent with a valid local tag -> dry-run reports the push and `--execute` pushes that existing tag with the App token, then verifies the remote object (recovery completes, not a dead end); remote absent with no local tag -> create, verify, push, verify. Verify: fixture-repo integration tests (create/verify/App-token push, existing-tag verify-only, divergent-tag fail, wrong-target refusal, recovery after later ordinary commits on `origin/main` still resolving the candidate on first-parent history, release-like merge message whose diff is not a valid release -> refusal, local-only-tag rerun push, unsigned refusal, missing-secret refusal, missing-key/wrong-fingerprint signing preflight refusal) with the token exchange stubbed, `node --test`; `src/lib/app-token.mjs` unit-tested (mint success, malformed key, API error).
- [x] T5 `check` command: `src/commands/check.mjs`, non-mutating setup validation (§8): three control files (CHANGELOG.md with `## [Unreleased]`, `release:verify` script declared, committed lockfile), secrets and variables presence by name via `gh api` (names only, never values), `release` Environment existence with required-reviewer protection (protection rules inspected via `gh api`; Environment present without required reviewer is a failure: no approval gate, T3/§10 step 5), App-tag path readiness (`NPM_RELEASE_FLOW_APP_ID` variable present via `gh api`; `NPM_RELEASE_FLOW_APP_PRIVATE_KEY` non-empty in the local environment; App installation resolves for the repository via `gh api`, T11), local git/gh state (`gh auth status`, git identity, local GPG available, fingerprint env 40-hex), Git's configured commit-signing key locally available and usable, and a usable secret key for `NPM_RELEASE_FLOW_GPG_FINGERPRINT` (both read-only signing preflights, the same checks `prepare`/`tag` gate on). Exit 0 all good; exit 1 listing every problem with the error-content contract; never mutates; `--execute` accepted as no-op. Verify: fixture tests with PATH-shimmed `gh` (present/absent secret and variable cases; Environment present with and without required-reviewer protection; App variable present/absent, App private key present/absent in env; missing signing key and wrong-fingerprint cases), `node --test`.
- [x] T6 CLI wiring: replace `bin/npm-release-flow.mjs` placeholder: `node:util` `parseArgs`, dispatch to the three commands, usage + exit 1 for no/unknown subcommand or unknown flag, exit-code plumbing (0/1/2), `--execute` flag parsing; `src/index.mjs` exports the public surface (bin `main` + shared libs). Verify: bin-level `node --test` tests asserting exit codes and stderr usage/error-content output; existing ci.yml placeholder smoke (bin no longer exits 1 unconditionally) updated to a real smoke (e.g. no-args -> exit 1 with usage).
- [x] T7 detect job: `src/detect.mjs` (env-driven: `BEFORE_SHA`, `GITHUB_SHA`, `GH_TOKEN`; binds HEAD to `GITHUB_SHA`; validates the §4 mandatory consumer prerequisites (CHANGELOG.md with `## [Unreleased]`, `release:verify` script declared in package.json, committed lockfile) before emitting any verdict, ordinary or release; any missing prerequisite -> hard fail (missing ⇒ detect fails, §4); classifies per §9 using `src/lib/release-state.mjs` (ordinary push -> `is-release=false`, exit 0, no PR or skew-marker lookup; valid release -> `is-release=true` + version); writes `is-release`/`version` to `GITHUB_OUTPUT`; exit 0 ordinary/valid, 1 hard fail) plus the §10 skew-marker read, performed only on the valid-release branch (resolve merged PR number from the triggering commit's merge message, `gh api` PR body, extract the `Kit: @vipentti/npm-release-flow@<version>` marker, compare with `.npm-release-flow/package.json` version; unparseable merge message, unreadable marker, or mismatch -> hard fail). Job body: env wiring (`BEFORE_SHA`, `GH_TOKEN`) around the existing scaffolded step, `GITHUB_OUTPUT` plumbing already declared; the job permission block stays exactly `contents: read` (§5), and the §10 PR-body read uses that contents-read token. Verify: `node --test` classification matrix (every §9 row) incl. marker-present/missing/mismatch cases with a stubbed `gh`; an ordinary compliant fixture that exits 0 with zero PR API calls (stubbed `gh` records zero calls); ordinary fixtures missing each mandatory prerequisite (no CHANGELOG.md, missing or malformed `## [Unreleased]` heading, no `release:verify` script, no lockfile) that exit 1 with zero PR API calls; passing coverage for an empty `[Unreleased]` body (the expected post-prepare state per the §4 changelog grammar) alongside a valid released-version section; actionlint clean; step wiring matches §5 outputs; workflow review confirming the detect job stays exactly `contents: read` (§5).
- [x] T8 verify job: a setup-node step (`actions/setup-node@820762786026740c76f36085b0efc47a31fe5020`, v7.0.0) before `npm ci` selects the consumer's Node from `package.json.engines.node` with Node 22 fallback when absent (§7); `src/verify.mjs` (env `VERSION`, `GH_TOKEN`, `CALLER_REPOSITORY`): `npm ci`, `npm run release:verify`, defensive re-validation of the §4 mandatory checks (enforcement lives in detect, §4), build-if-declared, bin-entry checks, create a dedicated pack directory and run `npm pack --json --ignore-scripts --pack-destination <pack-dir>`, persist the exact JSON report as `<pack-dir>/pack.json`, validate the tarball from that directory (pack contract: exactly one tarball, safe filename, name/version match manifest and release version, integrity matches the bytes), generic smoke (`npm i <tgz> --ignore-scripts --omit=peer` into a temp project, declared binaries resolve), T9 skew check with the self-host path explicit (normal consumer: installed devDependency version must equal the checkout's version; self-host, `CALLER_REPOSITORY` == kit repository derived from the checkout's `package.json` `repository.url`: no self devDependency required, pin agreement proven by the §10 prepare-marker vs workflow-checkout check in detect), `package-sha256` to `GITHUB_OUTPUT`. Job body: `actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a` (v7.0.1) step (name `npm-release-flow-${{ github.sha }}`, uploading exactly the pack-dir tarball plus `pack.json`, `if-no-files-found: error`). Verify: fixture consumer repo run (passing and failing fixtures: missing mandatory file, version mismatch, skewed pin), output asserted; a self-host fixture verifying with no installed kit copy passes via the marker path; both pack-dir files (tarball + `pack.json`) exist before upload; both verify-job Node cases exercised (declared `engines.node` and absent -> Node 22 fallback); actionlint clean; setup-node and upload-artifact reviewed at their exact pinned SHAs.
- [ ] T9 release job body: replace placeholder steps with the §5-§9 step order: consumer checkout at the triggering SHA first (`actions/checkout` pinned, `ref: ${{ github.sha }}`, `fetch-depth: 0`, `persist-credentials: false`), before any probing or revalidation (tag probe, revalidation, changelog access, and release mutations all need consumer git state at the workspace root), then the existing guarded kit checkout into `.npm-release-flow` at `job.workflow_sha`; setup-node pinned at `820762786026740c76f36085b0efc47a31fe5020` (v7.0.0) for exact Node 24.11.1 with `registry-url: https://registry.npmjs.org` (§7; npm 11.6.2 is that Node release's bundled npm, no `npm-version` input exists; the registry pin keeps the protected path on npmjs.com regardless of consumer npm config) with exact-version assertion of both `node --version == v24.11.1` and `npm --version == 11.6.2` (§7, fail closed, before any mutation); tag probe (`git ls-remote` -> `tag-exists`, step id `tag-probe`; result consumed later, no re-probe); revalidation #1 via `src/revalidate.mjs` (ancestry + control-file equality, `GH_TOKEN` only); `actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c` (v8.0.1) with `name: npm-release-flow-${{ github.sha }}` (exactly the uploaded artifact, never the run's other artifacts) and an explicit dedicated `path`; `src/validate-artifact.mjs` with `PACKAGE_SHA256: ${{ needs.verify.outputs.package-sha256 }}` (named env var; consumes the download directory and fails unless it contains exactly the expected tarball and `pack.json`; sha256 equals the output, pack.json contract, writes `PACKAGE_TARBALL` to `$GITHUB_ENV`); fresh `GNUPGHOME` init (mktemp, chmod 700, `$GITHUB_ENV`); public-key config step (only `tag-exists=true`: step-scoped non-empty assertion of `NPM_RELEASE_FLOW_GPG_PUBLIC_KEY`, then import, fingerprint 40-hex, exactly one primary public key, no secret keys); private-key config step (only `tag-exists=false`: step-scoped non-empty assertions of `NPM_RELEASE_FLOW_GPG_PRIVATE_KEY` and `NPM_RELEASE_FLOW_GPG_PASSPHRASE`, passphrase chmod 600, import, one primary secret key, fingerprint match, gpg-wrapper + git identity from `NPM_RELEASE_FLOW_GIT_NAME`/`GIT_EMAIL`); revalidation #2 (same script, immediately before tag creation); step-scoped non-empty assertion of `NPM_RELEASE_FLOW_APP_PRIVATE_KEY` immediately before `actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1` (v3.2.0; only `tag-exists=false`; `NPM_RELEASE_FLOW_APP_ID` var, T11); `src/release.mjs` mutation step with named env `VERSION: ${{ needs.detect.outputs.version }}`, `TAG_EXISTS: ${{ steps.tag-probe.outputs.tag-exists }}`, `NPM_RELEASE_FLOW_GPG_FINGERPRINT: ${{ vars.NPM_RELEASE_FLOW_GPG_FINGERPRINT }}`, `NPM_RELEASE_FLOW_APP_TOKEN: ${{ steps.release-app-token.outputs.token }}` (only when `tag-exists=false`), `GH_TOKEN: ${{ github.token }}` (`GNUPGHOME`/`PACKAGE_TARBALL` inherited via `$GITHUB_ENV`); cleanup step `if: always()` (passphrase + home removed). All secrets step-scoped, loaded only after revalidation #1 (§6); every secret asserted non-empty in step-scoped checks after non-secret validation (§10); private material never loaded on verify-only reruns. Verify: actionlint clean; step order/names and secret scoping reviewed against §5/§6/§7/§9/§10, including the consumer checkout preceding the tag probe and revalidation #1; setup-node, download-artifact, and create-github-app-token reviewed at their exact pinned SHAs; setup-node `registry-url` pin present and no other registry source in the job (review).
- [ ] T10 `src/release.mjs` mutation orchestration (§9 boundaries 4-6, env-driven, all inputs wired from T9's named env vars, none guessed at runtime: `VERSION`, `TAG_EXISTS` from the `tag-probe` output (used as-is; no pre-mutation re-probe), `GITHUB_SHA`, `GITHUB_REPOSITORY`, `GNUPGHOME` + `PACKAGE_TARBALL` via `$GITHUB_ENV`, `NPM_RELEASE_FLOW_GPG_FINGERPRINT`, `NPM_RELEASE_FLOW_APP_TOKEN` (only when `tag-exists=false`), `GH_TOKEN`): boundary 4 tag push (create-then-verify-then-push, App-token-authenticated push via git extraheader config, `ls-remote` equality; tag present -> fetch + verify-only, private material never loaded; divergent -> hard fail), GitHub API signature-verification poll (`git/tags/<object>`, polled, `.verification.verified == true`), boundary 5 publish (`npm view --registry=https://registry.npmjs.org` verify-or-idempotent: present -> identity/integrity verify; absent -> refuse if newer stable version exists, else `npm publish <tarball> --ignore-scripts --access public --provenance --registry=https://registry.npmjs.org`, wait for packument visibility, same verify: name/version/repository match, `dist.integrity` equals packed integrity, `gitHead` only when present), boundary 6 GitHub Release (notes from the changelog section via `src/lib/changelog.mjs`; `gh release view` present -> `edit`, absent -> `create --verify-tag`). Verify: unit tests on the mutation helpers with stubbed git/gh/npm executables (create/push/verify, verify-only path, divergent tag, E404 refuse-newer, publish-then-verify, release create/edit); a test proving a consumer `.npmrc` pointing at another registry cannot redirect protected `npm view`/`npm publish` (stubbed npm records the `--registry` argument); actionlint unaffected.
- [ ] T11 End-to-end integration: run the full CLI and job-script surface against a fresh fixture consumer repo: `prepare` dry-run + execute (bare remote, stubbed `gh`), `tag` create/push/verify-only, `check` pass/fail, `detect` against fixture histories, `verify` against a fixture consumer, exit codes 0/1/2 and error-content strings asserted end to end; `npm pack --dry-run` lists all new `src`/`bin` files and the workflow; `npm run typecheck` and the pinned actionlint on both workflow files pass locally; `planlet validate cli-implementation` passes; CI green on the implementation branch. The kit's first self-release is not reachable from this planlet alone: it requires the out-of-scope self-release bootstrap (the kit's own `CHANGELOG.md` with `## [Unreleased]`, its `release:verify` script plus lockfile update, and the thin self-release caller workflow with the immutable workflow pin); end-to-end verification here covers fixture consumers only.
