# Tasks: Main protection ruleset: admin-role bypass

- [x] T1 Persist this planlet and apply the ruleset amendment: PUT ruleset `20612037` on `vipentti/npm-release-flow` via `gh api --method PUT /repos/vipentti/npm-release-flow/rulesets/20612037 --input ruleset.json`, where `ruleset.json` is initial-setup T5's writable ruleset body (name `main protection`, target branch, enforcement active, conditions ref_name include `refs/heads/main` / exclude empty, pull_request rule with all five parameters and `required_approving_review_count: 1`, required_status_checks with context `ci` and strict policy true) plus ONLY `"bypass_actors": [{"actor_id": 5, "actor_type": "RepositoryRole", "bypass_mode": "always"}]`. Verify: `gh api /repos/vipentti/npm-release-flow/rulesets/20612037 --jq '.bypass_actors'` returns exactly the RepositoryRole actor_id 5 entry with `bypass_mode: "always"`.
- [x] T2 Verify the amended ruleset end to end, read-only. Main ruleset: `gh api /repos/vipentti/npm-release-flow/rulesets/20612037 --jq '{name, target, enforcement, conditions, rules, bypass_actors}'` matches the COMPLETE expected amended T5 configuration: name `main protection`, target branch, enforcement active, conditions ref_name include `["refs/heads/main"]` / exclude `[]`, pull_request rule with all five parameters (dismiss_stale_reviews_on_push false, require_code_owner_review false, require_last_push_approval false, required_approving_review_count 1, required_review_thread_resolution false), required_status_checks with context `ci` and strict_required_status_checks_policy true, and bypass_actors exactly `[{"actor_id": 5, "actor_type": "RepositoryRole", "bypass_mode": "always"}]`. Tag rulesets separately: `gh api /repos/vipentti/npm-release-flow/rulesets/20612441 --jq '{name, target, enforcement, rules, bypass_actors}'` shows `release-tag creation` target tag, enforcement active, rule `creation`, bypass actor Integration `4497864` always; `gh api /repos/vipentti/npm-release-flow/rulesets/20612046 --jq '{name, target, enforcement, rules}'` shows `release-tag immutability` target tag, enforcement active, rules `update` (parameter `update_allows_fetch_and_merge` is false, the API default, and elided from the GET response) / `deletion` / `non_fast_forward`.
- [x] T3 Deliver the planlet-only commit and align task state with committed state: commit only the planlet files (`plans/main-protection-bypass/plan.md`, `plans/main-protection-bypass/tasks.md`) and push the `fm/npm-release-flow-main-bypass` branch; no product code changes. Verify: `git show --name-only --stat HEAD` lists ONLY the two planlet files, the branch is pushed and matches `origin/fm/npm-release-flow-main-bypass`, and `planlet validate main-protection-bypass` passes; after those checks pass, mark T3 complete with `planlet task check main-protection-bypass T3` and commit + push the resulting checklist update so task state does not trail committed state.

## Completion

- Completed at: 2026-08-10T16:10:20.527Z
- Mode: normal
