# Tasks: Public release preparation and self-release bootstrap

- [ ] T1 Test script and formatting baseline: add `"test": "node --test"` to `package.json` scripts (the only intentional package.json change in this task), then run `npm run format` so the tree is prettier-clean; review the diff and confirm it is formatting-only (whitespace, quoting, line-wrapping) with no behavior change, excluding the intentional `test`-script addition. Do not reference `CHANGELOG.md`/`SECURITY.md` in the format scripts yet: those files do not exist until T4/T7, and prettier fails on non-existent literal targets; T7 adds them once both exist. Verify: `npm run format:check` exits 0 with the current file lists; `npm run lint` and `npm run typecheck` still pass; the format diff is reviewed.
- [ ] T2 Windows test-harness fix: repair the pre-existing bug in `test/helpers/fixture.mjs` where the GNUPGHOME temp path is misread as cwd-relative by the Git-bundled gpg on Windows (the failing tests show `gpg: keyblock resource '<cwd>/C:\...' No such file or directory` and `gpg: key generation failed: No agent running`), so the fixture signing home works on this Windows machine; POSIX behavior must be unchanged (branch only on win32). Verify: `npm test` passes in full locally on Windows, including the check/prepare/tag/verify/e2e suites that currently fail.
- [ ] T3 Harden `ci.yml`: in the single job still named `ci`, add `npm run format:check`, `npm run knip`, and `npm test` steps in this order: install (npm ci), lint, format check, typecheck, knip, tests, workflow lint (pinned actionlint v1.7.12 with the established `-ignore` patterns plus the targeted `unexpected key "queue" for "concurrency" section` ignore required by the caller's `concurrency.queue: max`, which v1.7.12 predates), package/CLI smoke; keep all existing steps and the existing actionlint acquisition exactly, but widen the actionlint invocation to lint all `.github/workflows/*.yml` (so the new caller workflow added in T9 is covered by CI, not only locally). Verify: actionlint is clean on every `.github/workflows/*.yml` (with the three ignore patterns); the step order matches this list; the job name and required check context `ci` are unchanged.
- [ ] T4 Add `CHANGELOG.md`: Keep a Changelog style with `# Changelog` and a non-empty `## [Unreleased]` section whose initial entries summarize the implemented functionality (the CLI `prepare`/`tag`/`check` commands, the reusable `release.yml` workflow, and the security/trust properties as implemented: signed tags, App-only tag push, Trusted Publishing/provenance, unprivileged verify job); no released-version section. Verify: the `## [Unreleased]` section is non-empty and parseable by the kit's own changelog logic; the content matches the implemented surface (derive from `src/` and the T6 README draft).
- [ ] T5 Add `release:verify` npm script to `package.json` composing the repository's authoritative pre-release checks in a deterministic order: `npm run lint`, `npm run format:check`, `npm run typecheck`, `npm run knip`, `npm test`; update `package-lock.json` if the package.json change requires it. Verify: `npm run release:verify` exits 0 locally; the script composes existing scripts rather than duplicating commands; lockfile is committed and in sync.
- [ ] T6 Rewrite `README.md` as self-contained public documentation with no dependence on `vipentti/agent-blueprints` (public readers must not need the private repo): what the kit is; current project/release status; supported Node version (`>=22.14.0`, from `engines`); installation (and intended installation once published); CLI commands `prepare`/`tag`/`check` with the dry-run default and `--execute`; the high-level release flow; reusable GitHub Actions workflow usage with the exact caller contract (top-level `permissions: {}`, `concurrency` group `release-main` with `cancel-in-progress: false` and `queue: max`, calling job `uses` at a full 40-character SHA, calling-job permissions `contents: write` + `pull-requests: read` + `id-token: write`, exactly the four `NPM_RELEASE_FLOW_*` secrets by name, no inputs); required consumer prerequisites (the exact `@vipentti/npm-release-flow` devDependency version, which must correspond to the workflow's pinned commit and move with it in one upgrade PR; src/verify.mjs hard-fails a normal consumer whose installed kit copy is absent or version-mismatched, so this pin is mandatory, not optional; CHANGELOG.md with `## [Unreleased]`, `release:verify` script, committed `package-lock.json`, `release` Environment, required secrets/variables, GitHub App, npm Trusted Publishing matching the caller's workflow filename); a security/trust model summary (consumer code runs only in the unprivileged verify job, the protected release job does not install consumer dependencies, release secrets are scoped late, tags are GPG-signed, the GitHub App is the tag-push identity, npm publication uses Trusted Publishing/provenance); a link to `RELEASE.md`; development commands; license. The blueprint pin is retained only as a clearly-marked historical/provenance note, never as a requirement to consult. Verify: every CLI/workflow/security claim matches `bin/npm-release-flow.mjs`, `src/commands/*`, `.github/workflows/release.yml`, and the tests (derive, do not invent); the documented caller contract is identical to the T9 caller; no unresolved reference to the private blueprint; prettier-clean.
- [ ] T7 Add `SECURITY.md`, concise: a supported-versions policy appropriate for a project before its first stable release; a request that vulnerabilities not be filed publicly; GitHub private vulnerability reporting as the preferred path (fallback: contact the maintainer privately, without inventing an email address); scope examples relevant to this project (release workflow privilege boundaries, credential/secret exposure, GitHub App token handling, GPG signing, npm Trusted Publishing/provenance, tag protections, release validation bypasses). Also extend the `format`/`format:check` scripts' file lists to include `CHANGELOG.md` (created in T4) and `SECURITY.md`, both of which now exist, then run `npm run format` and review any diff as formatting-only. Verify: the content is accurate against the implemented security model (consistent with the T6 trust summary); no fabricated contact details; `npm run format:check` passes with the extended file lists.
- [ ] T8 Restructure `RELEASE.md` to be self-contained and accurate after these changes: copy all user-relevant requirements into the file so no reader must consult `vipentti/agent-blueprints` (the pinned reference stays only as optional provenance/history); separate "Automated/repository state" (what is implemented: workflow, CLI, CI, rulesets, `release` Environment exists without the reviewer rule, CHANGELOG/release:verify present) from "Human-required setup"; record the exact human sequence before going public and the exact bootstrap order before the first npm release, in this order: complete the pre-public checks from this planlet's verification; make the repository public; remove `"private": true` in an ordinary PR while the version stays `0.0.0` (the release-diff allowlist permits only the three control files, so removing it inside the release PR would invalidate the release diff); run `prepare` for the first version from that `0.0.0` main (so the PR marker stays `0.0.0` and matches the caller pinned at the `0.0.0` kit); manually publish the prepared version before the release PR merge (a brand-new package cannot OIDC-publish its first version), using a procedure proven against the release verify logic: `publishedIdentityProblems` (src/release.mjs, covered in test/release.test.mjs) checks name/version/repository/integrity and checks `gitHead` only when present, against the eventual merge commit which does not exist at manual-publish time, so the manual publish must be a tarball-path publish of the exact prepared tarball (npm records no `gitHead` for tarball-path publishes) followed by a registry-metadata verification (name, version, `dist.integrity`, no incompatible `gitHead`) before the merge; do not assume idempotency, trace the procedure against the verify code and tests; configure the Trusted Publisher for `.github/workflows/self-release.yml` (npm matches the caller's filename), the required secrets/variables, the required reviewer on the `release` Environment (exact `gh api` command), and the App installation; then merge the release PR so its `0.0.0` marker matches the pinned workflow (the tag/publish steps are verify-or-idempotent against the manual publish); afterward advance the caller pin in an ordinary PR; document the caller pin-advance procedure (after each self-release, advance the pin in a follow-up ordinary PR, always to a full 40-character SHA) and the accurate secret contract (ordinary pushes are non-release detection runs and stay green; release attempts cannot complete until secrets exist). Verify: RELEASE.md requires no access to the private blueprint; the manual-publish procedure is traced against `publishedIdentityProblems` and its tests so the documented registry state is accepted by the verify step; the sequences match the actual repository and settings state; prettier-clean.
- [ ] T9 Add the self-release caller workflow `.github/workflows/self-release.yml` with the exact contract: trigger on push to `main`; top-level `permissions: {}`; `concurrency` group `release-main` with `cancel-in-progress: false` and `queue: max` (the default `queue: single` replaces a pending run when a newer run arrives in the same group, silently dropping a release-triggering push; `queue: max` allows up to 100 pending runs and avoids the default single-pending replacement behavior, substantially reducing the risk of dropping a release-triggering push); one job calling `vipentti/npm-release-flow/.github/workflows/release.yml@764e09cf642997b736663e1711e69bbb6d71a43e` with job `permissions: { contents: write, pull-requests: read, id-token: write }` (the union the kit jobs need; the kit narrows per job) and passing exactly the four `NPM_RELEASE_FLOW_*` secrets by name (matching release.yml's `workflow_call` interface exactly, never `secrets: inherit`); no inputs, no copied release logic, no secret values. Verify: actionlint is clean (via the widened `.github/workflows/*.yml` invocation from T3, with the established `-ignore` patterns plus the targeted `unexpected key "queue" for "concurrency" section` ignore for the `queue` key); the pin is the full 40-character SHA `764e09cf642997b736663e1711e69bbb6d71a43e` holding the kit at version `0.0.0`; the contract (permissions, concurrency, secrets, no inputs) matches the README contract from T6 and the release.yml `workflow_call` interface exactly; RELEASE.md (T8) documents the pin-advance procedure and the accurate secret contract; the actual dispatch behavior of the caller with unset secrets is checked (an ordinary push should remain a non-release detection run) and the result is recorded in the T10 report.
- [ ] T10 Final verification and report: run the complete local verification (`npm ci`, `npm run lint`, `npm run format:check`, `npm run typecheck`, `npm run knip`, `npm test`), run `planlet validate public-release-bootstrap`, run the repository's pinned actionlint procedure against all `.github/workflows/*.yml` (including the new caller), run `npm pack --dry-run` and inspect the listing for unintended files, secrets, test fixtures, temporary artifacts, or dev-only files, and run a gitleaks (8.30.1, available locally) history scan on a full local clone. Produce the final report covering: (1) files changed, (2) checks run and results, (3) anything deliberately left for the human, (4) the exact human sequence before making the repository public, (5) the exact human sequence before the first npm release, (6) any blocker discovered. Verify: every required check passes (lint, format:check, typecheck, knip, test, actionlint, pack inspection, `planlet validate`); the only permitted exception is an unavailable or incomplete gitleaks scan, which must be reported and completed by the human before the visibility change; the pack listing is intentional; no visibility, secret, environment, npm, or release mutation was performed.
