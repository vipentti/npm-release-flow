# Tasks: Public release preparation and self-release bootstrap

- [ ] T1 Test script and formatting baseline: add `"test": "node --test"` to `package.json` scripts (no other package.json change), then run `npm run format` so the tree is prettier-clean; review the diff and confirm it is formatting-only (whitespace, quoting, line-wrapping) with no behavior change. Verify: `npm run format:check` exits 0; `npm run lint` and `npm run typecheck` still pass; the format diff is reviewed.
- [ ] T2 Windows test-harness fix: repair the pre-existing bug in `test/helpers/fixture.mjs` where the GNUPGHOME temp path is misread as cwd-relative by the Git-bundled gpg on Windows (the failing tests show `gpg: keyblock resource '<cwd>/C:\...' No such file or directory` and `gpg: key generation failed: No agent running`), so the fixture signing home works on this Windows machine; POSIX behavior must be unchanged (branch only on win32). Verify: `npm test` passes in full locally on Windows, including the check/prepare/tag/verify/e2e suites that currently fail.
- [ ] T3 Harden `ci.yml`: in the single job still named `ci`, add `npm run format:check`, `npm run knip`, and `npm test` steps in this order: install (npm ci), lint, format check, typecheck, knip, tests, workflow lint (pinned actionlint v1.7.12 with the established `-ignore` patterns), package/CLI smoke; keep all existing steps and the existing actionlint acquisition/invocation exactly. Verify: actionlint is clean on `ci.yml` and `release.yml`; the step order matches this list; the job name and required check context `ci` are unchanged.
- [ ] T4 Add `CHANGELOG.md`: Keep a Changelog style with `# Changelog` and a non-empty `## [Unreleased]` section whose initial entries summarize the implemented functionality (the CLI `prepare`/`tag`/`check` commands, the reusable `release.yml` workflow, and the security/trust properties as implemented: signed tags, App-only tag push, Trusted Publishing/provenance, unprivileged verify job); no released-version section. Verify: the `## [Unreleased]` section is non-empty and parseable by the kit's own changelog logic; the content matches the implemented surface (derive from `src/` and the T6 README draft).
- [ ] T5 Add `release:verify` npm script to `package.json` composing the repository's authoritative pre-release checks in a deterministic order: `npm run lint`, `npm run format:check`, `npm run typecheck`, `npm run knip`, `npm test`; update `package-lock.json` if the package.json change requires it. Verify: `npm run release:verify` exits 0 locally; the script composes existing scripts rather than duplicating commands; lockfile is committed and in sync.
- [ ] T6 Rewrite `README.md` as self-contained public documentation with no dependence on `vipentti/agent-blueprints` (public readers must not need the private repo): what the kit is; current project/release status; supported Node version (`>=22.14.0`, from `engines`); installation (and intended installation once published); CLI commands `prepare`/`tag`/`check` with the dry-run default and `--execute`; the high-level release flow; reusable GitHub Actions workflow usage (no inputs, the four `NPM_RELEASE_FLOW_*` secrets, outputs); required consumer prerequisites (CHANGELOG.md with `## [Unreleased]`, `release:verify` script, committed `package-lock.json`, `release` Environment, required secrets/variables, GitHub App, npm Trusted Publishing); a security/trust model summary (consumer code runs only in the unprivileged verify job, the protected release job does not install consumer dependencies, release secrets are scoped late, tags are GPG-signed, the GitHub App is the tag-push identity, npm publication uses Trusted Publishing/provenance); a link to `RELEASE.md`; development commands; license. The blueprint pin is retained only as a clearly-marked historical/provenance note, never as a requirement to consult. Verify: every CLI/workflow/security claim matches `bin/npm-release-flow.mjs`, `src/commands/*`, `.github/workflows/release.yml`, and the tests (derive, do not invent); no unresolved reference to the private blueprint; prettier-clean.
- [ ] T7 Add `SECURITY.md`, concise: a supported-versions policy appropriate for a project before its first stable release; a request that vulnerabilities not be filed publicly; GitHub private vulnerability reporting as the preferred path (fallback: contact the maintainer privately, without inventing an email address); scope examples relevant to this project (release workflow privilege boundaries, credential/secret exposure, GitHub App token handling, GPG signing, npm Trusted Publishing/provenance, tag protections, release validation bypasses). Verify: the content is accurate against the implemented security model (consistent with the T6 trust summary); no fabricated contact details.
- [ ] T8 Restructure `RELEASE.md` to be self-contained and accurate after these changes: copy all user-relevant requirements into the file so no reader must consult `vipentti/agent-blueprints` (the pinned reference stays only as optional provenance/history); separate "Automated/repository state" (what is implemented: workflow, CLI, CI, rulesets, `release` Environment exists without the reviewer rule, CHANGELOG/release:verify present) from "Human-required setup" (make the repo public, add the required reviewer to the `release` Environment with the exact `gh api` command, create secret/variable values, install/verify the GitHub App, configure npm Trusted Publisher, first manual npm publication, remove `"private": true`, set the first release version); record the exact human sequence before going public and before the first npm release; document the caller pin-advance procedure (after each self-release, advance the pin in a follow-up ordinary PR) and the pre-secrets failure window for the caller workflow. Verify: RELEASE.md requires no access to the private blueprint; the sequences match the actual repository and settings state; prettier-clean.
- [ ] T9 Add the self-release caller workflow `.github/workflows/self-release.yml`: trigger on push to `main`; minimal permissions (no `secrets: inherit`); one job calling `vipentti/npm-release-flow/.github/workflows/release.yml@764e09c` and passing exactly the four `NPM_RELEASE_FLOW_*` secrets by name (matching release.yml's `workflow_call` interface exactly); no inputs, no copied release logic, no secret values. Verify: actionlint is clean (with the established `-ignore` patterns); the pin equals the current main SHA holding the kit at version `0.0.0`; the four secret names match `release.yml` exactly; RELEASE.md (T8) documents the pin-advance procedure and the pre-secrets failure window.
- [ ] T10 Final verification and report: run the complete local verification (`npm ci`, `npm run lint`, `npm run format:check`, `npm run typecheck`, `npm run knip`, `npm test`), run the repository's pinned actionlint procedure against all workflow files (including the new caller), run `npm pack --dry-run` and inspect the listing for unintended files, secrets, test fixtures, temporary artifacts, or dev-only files, and run a gitleaks (8.30.1, available locally) history scan on a full local clone. Produce the final report covering: (1) files changed, (2) checks run and results, (3) anything deliberately left for the human, (4) the exact human sequence before making the repository public, (5) the exact human sequence before the first npm release, (6) any blocker discovered. Verify: all checks green (or the failure is a documented pre-existing/environmental finding with a stated disposition); the pack listing is intentional; the gitleaks result is reported (if the scan cannot complete, state that the human must run the history scan before the visibility change); no visibility, secret, environment, npm, or release mutation was performed.
