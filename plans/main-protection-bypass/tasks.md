# Tasks: Main protection ruleset: admin-role bypass

- [x] T1 Persist this planlet and apply the ruleset amendment: PUT ruleset `20612037` on `vipentti/npm-release-flow` via the gh-axi raw API (`gh-axi api PUT /repos/vipentti/npm-release-flow/rulesets/20612037 --field body=@ruleset.json`), where `ruleset.json` is the existing GET body plus `"bypass_actors": [{"actor_id": 5, "actor_type": "RepositoryRole", "bypass_mode": "always"}]` and nothing else changed. Verify: `gh-axi api GET /repos/vipentti/npm-release-flow/rulesets/20612037 --jq '.bypass_actors'` returns the RepositoryRole actor_id 5 entry with `bypass_mode: "always"`.
- [x] T2 Verify the amended ruleset end to end, read-only: `gh-axi api GET /repos/vipentti/npm-release-flow/rulesets/20612037 --jq '.enforcement, .conditions.ref_name.include, [.rules[] | {type, parameters: (.parameters // {})}]'` shows enforcement `active`, conditions including `refs/heads/main`, `pull_request` rule with `required_approving_review_count: 1`, and `required_status_checks` with context `ci` still present. Also confirm via `gh-axi api GET /repos/vipentti/npm-release-flow/rulesets --jq '.[] | select(.name == "main protection") | {id, target, enforcement}'` that the main ruleset is the only one with `target: branch` and the tag rulesets are untouched.
- [ ] T3 Commit only the planlet files (`plans/main-protection-bypass/plan.md`, `plans/main-protection-bypass/tasks.md`) and push the `fm/npm-release-flow-main-bypass` branch; no product code changes. Verify: `git status` shows exactly the two planlet files staged/committed, `git show --stat HEAD` lists only those files, and the push reports the new branch upstream.
