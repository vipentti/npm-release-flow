# Tasks: Initial setup of the npm-release-flow kit

Blueprint refs (§N) throughout: `vipentti/agent-blueprints` @ `f9d06c77b9920e4cfab774d254f22d60894c1f05`, file `blueprints/npm-release-flow.md`.

- [ ] T1 Package skeleton: package.json (name `@vipentti/npm-release-flow`, `private: true`, version 0.0.0, `type: module`, `engines.node >= 22.14.0`, `main: "./src/index.mjs"`, root `exports` -> `"./src/index.mjs"`, `bin.npm-release-flow: "./bin/npm-release-flow.mjs"` (exact required mappings), `files` bin/src/workflow, `license: MIT`, `repository: { type: "git", url: "git+https://github.com/vipentti/npm-release-flow.git" }` (exact required value, §10), zero runtime deps), MIT `LICENSE` file (`Copyright (c) 2026 Ville Penttinen`), `src/index.mjs` and `bin/npm-release-flow.mjs` placeholders (usage, exit 1), `.gitignore`, committed `package-lock.json`. Verify: `npm ci` succeeds, `npm pack --dry-run` lists only intended files plus package.json/README/LICENSE, `repository.url` matches `git+https://github.com/vipentti/npm-release-flow.git` exactly, package.json contains exactly `main: "./src/index.mjs"`, root `exports` mapping to `"./src/index.mjs"`, and `bin.npm-release-flow: "./bin/npm-release-flow.mjs"`, `node bin/npm-release-flow.mjs` prints usage and exits 1.
- [ ] T2 Reusable workflow: `.github/workflows/release.yml` with the §5 interface (no inputs; four `NPM_RELEASE_FLOW_*` secrets `required: true`; outputs `is-release`/`version`; `permissions: {}`), jobs detect/verify/release with §5 per-job permissions (`contents: read`; `release` job gets `environment: release`, `contents: write`, `id-token: write` only there), kit-checkout guard and checkout at `job.workflow_sha` into `.npm-release-flow` using `actions/checkout` pinned to `3d3c42e5aac5ba805825da76410c181273ba90b1` (v7.0.1); job bodies invoke kit scripts at `.npm-release-flow/src/...`; packaged copy at `workflow/release.yml` byte-identical. Verify: actionlint clean on both files, `cmp` passes, interface matches the blueprint on review, both copies use exactly `actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1`.
- [ ] T3 CI and typecheck scaffolding: `tsconfig.json` with explicit scope (`include` `src/**/*.mjs` + `bin/**/*.mjs`; `checkJs`, `noEmit`, `allowJs`, `module`/`moduleResolution: NodeNext`, `target: "ES2023"` (matching the planlet TypeScript config), `strict`, `types: ["node"]`) and `npm run typecheck` = `tsc`; `.github/workflows/ci.yml` with a single job named `ci` (the T5 ruleset's required check context) on Node 24 running npm ci, `tsc` against `tsconfig.json`, actionlint pinned v1.7.12 via release-binary download (same acquisition and invocation in CI and locally, target files `.github/workflows/release.yml` + `.github/workflows/ci.yml`), workflow byte-equality check, pack smoke (`npm pack --dry-run`, stub exit 1). Verify: `npm run typecheck` (tsc against the defined `tsconfig.json`) and the pinned actionlint on both workflow files pass locally with zero errors; CI green on the implementation branch.
- [ ] T4 Release prerequisites documentation: `RELEASE.md` with the §6 secret/variable fixed names (`NPM_RELEASE_FLOW_GPG_PRIVATE_KEY`/`GPG_PASSPHRASE`/`GPG_PUBLIC_KEY`/`APP_PRIVATE_KEY` secrets; `GPG_FINGERPRINT`/`GIT_NAME`/`GIT_EMAIL`/`APP_ID` variables), §7 pins (protected Node 24.11.1 + npm 11.6.2, verify fallback Node 22), `release` Environment requirement, App installation (T11), trusted-publisher workflow-filename note, §10 first-release checklist; README links it. Verify: every §6 name and both §7 pins appear verbatim; README link resolves.
- [ ] T5 Repo release settings via gh-axi (npm package, pinned 0.1.30, invoked as `npx -y gh-axi`): resolve the owner's user id (`gh-axi api users/<owner>`), create `release` Environment with required reviewer (`gh-axi api -X PUT /repos/vipentti/npm-release-flow/environments/release`, body `{"protection_rules":[{"type":"required_reviewers","parameters":{"reviewer_ids":[<owner id>]}}]}`), create the main ruleset (`gh-axi api -X POST /repos/vipentti/npm-release-flow/rulesets`, body `{"name":"main protection","target":"branch","enforcement":"active","conditions":{"ref_name":{"include":["refs/heads/main"],"exclude":[]}},"rules":[{"type":"pull_request","parameters":{"required_approving_review_count":1}},{"type":"required_status_checks","parameters":{"strict_required_status_checks_policy":true,"contexts":["ci"]}}]}`). Verify via `gh-axi api`: environment GET shows `required_reviewers` with the owner, ruleset GET shows enforcement `active` with the `pull_request` and `required_status_checks` (contexts `["ci"]`) rules.
